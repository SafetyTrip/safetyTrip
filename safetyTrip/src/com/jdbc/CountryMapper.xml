<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="CountryMapper">
	<insert id="insertCountry" parameterType="CountryDTO">
		insert into country(couno, cname, cename)
		values(SEQ_COUNTRY_couno.nextval, #{cname}, #{cename})
	</insert>
	<select id="selectCountryByCname" parameterType="string" resultType="int">
		select couno from country where cname = #{cname}
	</select>
</mapper>



